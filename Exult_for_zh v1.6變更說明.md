# Exult_for_zh v1.6 變更說明

本版本重點包含**中/英文本動態切換**、**原生 `K` 鍵（鎖匙圈）功能整合**，以及**字體顯示與渲染算法全面優化**。

---

## 🌐 1. 多語系動態切換功能 (Chinese / English Switch)

現在玩家無需手動替換或刪除 Patch 檔案，即可直接在遊戲內選單自由切換中／英文本：

* **操作步驟**：
  1. 在主選單或遊戲中按下 `Esc` 鍵選單 ➔ 點選 **`SETUP`（設定）** ➔ 選擇 **`Game Display`（畫面與顯示設定）**。
  2. 找到 **`Language`（語言）** 選項：
     * **`Chinese`**：啟用繁體中文化模組（載入中文對話腳本、物品名稱、法術書與文字訊息），並自動連動將 `Built-in fonts` 強制設為 `Disabled` 鎖定，避免字體發生衝突。
     * **`English` / `Default`**：切換回原版英文文本與英文 Usecode 腳本，並自動解除 `Built-in fonts` 的鎖定。
  3. 按下下方 **`OK`** 儲存，遊戲會即時重新載入腳本與文本，**無需重新啟動遊戲**即可完成切換。

---

## 🔑 2. 原生支援 `K` 鍵（鎖匙圈快捷鍵）

本版本已將 **`K` 鍵（鑰匙圈快捷鍵）功能原生整合至 Exult 程式核心**：

* **無需額外安裝 MOD**：
  玩家無需再手動下載或安裝任何 Keyring 相關的修飾模組（Mod），只要使用本版 Exult 執行檔即可直接享有快捷功能。
* **與《巨蛇之島》(Serpent Isle) 完美相容**：
  本功能已針對《巨蛇之島》遊戲內建的「鑰匙圈」物品與 Usecode 邏輯進行相容處理，與遊戲原生鑰匙圈機制**完全相容、絕不衝突**。
* **便利開鎖與收納**：
  在遊戲中隨時按下 **`K`** 鍵，即可迅速自動開鎖或收納鑰匙。

---

## 🎨 3. 字體顯示與渲染演算法優化

* **「OP/ED/對話」與「書本/卷軸」雙字體顏色採樣並存**：
  * **對話／動畫／招牌**：採用全頁直方圖頻率統計算法，精確鎖定鮮艷明亮的黃/金色字體（調色盤 254）。
  * **書本／卷軸**：採用黑色墨水預設值，羊皮紙背景上文字恢復為正統純黑墨水（調色盤 255），解決捲軸文字變成淺黃色無法閱讀的問題。
* **`<small_font_path>` 脫離尺寸限制**：
  * 移除舊有 `font_size <= 10` 硬編碼門檻。只要在設定中指定了小字體檔案，所有書本、卷軸與介面文字會直接套用小字體。
* **對話框 CJK 行高修復**：
  * 修復語系切換後對話框中文字體與 `<line_spacing>` 的行高連動，避免對話文字重疊。
