name: ci-macos

on:
  push:
    paths:
      - "**"
      - "!android/**"
      - "!docs/**"
      - "docs/Makefile.am"
      - "!ios/**"
      - "!msvcstuff/**"
      - "!win32/**"
      - "!AUTHORS"
      - "!ChangeLog"
      - "!COPYING"
      - "!FAQ"
      - "!INSTALL"
      - "!NEWS"
      - "!**README**"
      - "!**.ico"
      - "!**.md"
      - "!**.png"
      - "!**.txt"
      - "!.clang*"
      - "!.gitignore"
      - "!.gitattributes"
      - "!.github/workflows/*"
      - ".github/workflows/ci-macos.yml"
  pull_request:
    paths:
      - "**"
      - "!android/**"
      - "!docs/**"
      - "docs/Makefile.am"
      - "!ios/**"
      - "!msvcstuff/**"
      - "!win32/**"
      - "!AUTHORS"
      - "!ChangeLog"
      - "!COPYING"
      - "!FAQ"
      - "!INSTALL"
      - "!NEWS"
      - "!**README**"
      - "!**.ico"
      - "!**.md"
      - "!**.png"
      - "!**.txt"
      - "!.clang*"
      - "!.gitignore"
      - "!.gitattributes"
      - "!.github/workflows/*"
      - ".github/workflows/ci-macos.yml"

jobs:
  ci-macos:
    runs-on: macos-latest
    steps:
      - name: Install dependencies
        run: |
          brew update
          brew upgrade || true
          brew install automake libtool pkg-config libpng libogg libvorbis fluid-synth freetype gtk+3 libxml2 bison flex
          brew install sdl3 sdl3_image icu4c mt32emu autoconf-archive dylibbundler create-dmg
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Run autoreconf
        run: |
          autoreconf -v -i
      - name: Configure
        run: |
          export LDFLAGS="-Wl,-headerpad_max_install_names -L$(brew --prefix bison)/lib -L$(brew --prefix flex)/lib -L$(brew --prefix freetype)/lib"
          export CPPFLAGS="-I$(brew --prefix flex)/include -I$(brew --prefix freetype)/include/freetype2"
          export PATH="$(brew --prefix bison)/bin:$(brew --prefix flex)/bin:$(brew --prefix libxml2)/bin:$PATH"
          export PKG_CONFIG_PATH="$(brew --prefix libxml2)/lib/pkgconfig:$(brew --prefix icu4c)/lib/pkgconfig:$(brew --prefix freetype)/lib/pkgconfig"
          export LIBS="-lfreetype"
          ./configure --with-debug=extreme --enable-exult-studio --enable-exult-studio-support --enable-compiler \
            --enable-zip-support --enable-shared --enable-shp-thumbnailer --enable-data --enable-mods --enable-aseprite-plugin \
            --disable-alsa --disable-timidity-midi
      - name: Build
        run: |
          export LDFLAGS="-Wl,-headerpad_max_install_names -L$(brew --prefix bison)/lib -L$(brew --prefix flex)/lib -L$(brew --prefix freetype)/lib"
          export CPPFLAGS="-I$(brew --prefix flex)/include -I$(brew --prefix freetype)/include/freetype2"
          export PATH="$(brew --prefix bison)/bin:$(brew --prefix flex)/bin:$(brew --prefix libxml2)/bin:$PATH"
          export PKG_CONFIG_PATH="$(brew --prefix libxml2)/lib/pkgconfig:$(brew --prefix icu4c)/lib/pkgconfig:$(brew --prefix freetype)/lib/pkgconfig"
          export LIBS="-lfreetype"
          make -j3
          make bundle_shared
          
          # Workaround for dylibbundler failing to rewrite Homebrew paths
          for lib in $(otool -L Exult.app/Contents/MacOS/exult | grep '/opt/homebrew' | awk '{print $1}'); do
            base=$(basename $lib)
            install_name_tool -change "$lib" "@executable_path/../Resources/lib/$base" Exult.app/Contents/MacOS/exult || true
          done
          for dylib in Exult.app/Contents/Resources/lib/*.dylib; do
            for lib in $(otool -L "$dylib" | grep '/opt/homebrew' | awk '{print $1}'); do
              base=$(basename $lib)
              install_name_tool -change "$lib" "@executable_path/../Resources/lib/$base" "$dylib" || true
            done
            # Re-sign the dylib if needed on ARM64
            codesign --force --sign - "$dylib" || true
          done
          # Re-sign the executable
          codesign --force --sign - Exult.app/Contents/MacOS/exult || true

          make osxdmg
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Exult-macOS-Build
          path: Exult*.dmg
  notify:
    if: ${{ always() }}
    needs:
      - ci-macos
    uses: ./.github/workflows/notify.yml
    with:
      build_name: "The Exult macOS build"
      build_result: ${{ needs.ci-macos.result }}
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
